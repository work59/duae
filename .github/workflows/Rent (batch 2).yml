name: Rent (batch 2)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  detect_pages:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.build_matrix.outputs.matrix }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Detect pages and build matrix
        id: build_matrix
        run: |
          python << 'EOF'
          import json
          import os
          import requests

          URL = "https://wd0ptz13zs-dsn.algolia.net/1/indexes/*/queries"
          PARAMS = {
              "x-algolia-agent": "Algolia for JavaScript (4.24.0); Browser (lite)",
              "x-algolia-api-key": "cdd839b4fdac840289e88633779e8634",
              "x-algolia-application-id": "WD0PTZ13ZS",
          }

          CATEGORIES = {
                "rent_commercial": {
                    "index": "property-for-rent-commercial.com",
                    "filter": '("category_v2.slug_paths":"property-for-rent/commercial")',
                    "hits": []
                },
                "rent_holiday_homes": {
                    "index": "property-for-rent-holiday-homes.com",
                    "filter": '("categories.slug_paths":"property-for-rent/holiday-homes")',
                    "hits": []
                },
                "rent_short_term_monthly": {
                    "index": "property-for-rent-short-term.com",
                    "filter": '("categories_v2.slug_paths":"property-for-rent/short-term")',
                    "hits": []
                },
                "rent_short_term_daily": {
                    "index": "property-for-rent-short-term-daily.com",
                    "filter": '("category_v2.slug_paths":"property-for-rent/short-term-daily")',
                    "hits": []
                }
          }

          PAGES_PER_JOB = 50

          matrix = {"include": []}

          for category_name, category in CATEGORIES.items():
              print(f"Detecting pages for {category_name}...")
              try:
                  payload = {
                      "requests": [{
                          "indexName": category["index"],
                          "query": "",
                          "params": f"page=0&hitsPerPage=35&filters={category['filter']}",
                      }]
                  }
                  r = requests.post(URL, params=PARAMS, json=payload, timeout=30)
                  data = r.json()
                  nb_pages = data["results"][0]["nbPages"]
                  #nb_pages = 2
                  print(f"  {category_name}: {nb_pages} pages")
              except Exception as e:
                  print(f"  Failed: {e}")
                  continue

              if nb_pages == 0:
                  continue

              for i in range(0, nb_pages, PAGES_PER_JOB):
                  start = i
                  end = min(i + PAGES_PER_JOB - 1, nb_pages - 1)
                  matrix["include"].append({
                      "category": category_name,
                      "start": start,
                      "end": end
                  })

          print(f"Total jobs: {len(matrix['include'])}")

          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f"matrix={json.dumps(matrix)}\n")
          EOF

  scrape:
    runs-on: ubuntu-latest
    needs: detect_pages
    strategy:
      matrix: ${{ fromJson(needs.detect_pages.outputs.matrix) }}
      max-parallel: 5
      fail-fast: false

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run scraper with retry
        run: |
          python -u main_prop.py "${{ matrix.category }}" ${{ matrix.start }} ${{ matrix.end }}

      - name: Upload raw data artifact
        uses: actions/upload-artifact@v4
        with:
          name: raw-${{ matrix.category }}-${{ matrix.start }}-${{ matrix.end }}
          path: |
            ${{ matrix.category }}_${{ matrix.start }}_${{ matrix.end }}.jsonl
            ${{ matrix.category }}_${{ matrix.start }}_${{ matrix.end }}_failed.txt
            ${{ matrix.category }}_${{ matrix.start }}_${{ matrix.end }}_result.json
            request_stats_${{ matrix.start }}_${{ matrix.end }}.json
          if-no-files-found: ignore

  # Extracts every unique agent/agency slug referenced across all raw
  # listings scraped by this workflow (agent_profile -> agent,
  # agent -> agency), so each profile is phone-scraped exactly once no
  # matter how many listings it appears on.
  combine_profiles:
    runs-on: ubuntu-latest
    needs: scrape
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas openpyxl

      - name: Download all raw data
        uses: actions/download-artifact@v4
        with:
          pattern: raw-*
          merge-multiple: true
          path: raw_parts/

      - name: Build unique agent/agency profile lists
        run: |
          python build_unique_profiles.py \
            --pattern "raw_parts/*.jsonl" \
            --agent-output combined_agent_profiles.xlsx \
            --agency-output combined_agency_profiles.xlsx

      - name: Upload combined profile lists
        uses: actions/upload-artifact@v4
        with:
          name: combined-profiles
          path: |
            combined_agent_profiles.xlsx
            combined_agency_profiles.xlsx
          if-no-files-found: ignore

  # Splits each of the two unique-profile files into 15-row chunks so the
  # phone_scrape job below can run one chunk per matrix entry.
  detect_phone_jobs:
    runs-on: ubuntu-latest
    needs: combine_profiles
    outputs:
      matrix: ${{ steps.build_matrix.outputs.matrix }}
    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas openpyxl

      - name: Download combined profile lists
        uses: actions/download-artifact@v4
        with:
          name: combined-profiles

      - name: Build phone-job matrix (15-row chunks)
        id: build_matrix
        run: |
          python << 'EOF'
          import json
          import os
          import pandas as pd

          CHUNK_SIZE = 15
          matrix = {"include": []}

          for profile_type, path in [("agent", "combined_agent_profiles.xlsx"),
                                      ("agency", "combined_agency_profiles.xlsx")]:
              if not os.path.exists(path):
                  continue
              df = pd.read_excel(path)
              n = len(df)
              print(f"{profile_type}: {n} unique profiles")
              for i in range(0, n, CHUNK_SIZE):
                  start = i
                  end = min(i + CHUNK_SIZE, n)
                  matrix["include"].append({
                      "profile_type": profile_type,
                      "start": start,
                      "end": end
                  })

          print(f"Total phone jobs: {len(matrix['include'])}")
          with open(os.environ['GITHUB_OUTPUT'], 'a') as fh:
              fh.write(f"matrix={json.dumps(matrix)}\n")
          EOF

  # Same Camoufox phone-reveal approach as the agency/agent scraper, run
  # 15 unique profiles at a time. max-parallel: 1 makes every chunk (both
  # agent and agency) run one after another.
  phone_scrape:
    runs-on: ubuntu-latest
    needs: detect_phone_jobs
    if: needs.detect_phone_jobs.outputs.matrix != '{"include":[]}'
    strategy:
      matrix: ${{ fromJson(needs.detect_phone_jobs.outputs.matrix) }}
      max-parallel: 1
      fail-fast: false
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-0 libdbus-glib-1-2

      - name: Install Camoufox
        run: python -m camoufox fetch

      - name: Download combined profile lists
        uses: actions/download-artifact@v4
        with:
          name: combined-profiles

      - name: Scrape phone numbers
        run: |
          python scraper_agency_agent.py \
            --input "combined_${{ matrix.profile_type }}_profiles.xlsx" \
            --profile-type ${{ matrix.profile_type }} \
            --start ${{ matrix.start }} \
            --end ${{ matrix.end }}

      - name: Upload phone results
        uses: actions/upload-artifact@v4
        with:
          name: phones-${{ matrix.profile_type }}-${{ matrix.start }}-${{ matrix.end }}
          path: ${{ matrix.profile_type }}_with_phone_${{ matrix.start }}_${{ matrix.end }}.csv
          if-no-files-found: ignore


  detect_merge_batches:
    runs-on: ubuntu-latest
    needs: scrape
    outputs:
      matrix: ${{ steps.build.outputs.matrix }}
    steps:
      - id: build
        run: |
          python << 'EOF'
          import json
          import os
          import math
          import requests

          URL = "https://wd0ptz13zs-dsn.algolia.net/1/indexes/*/queries"
          PARAMS = {
              "x-algolia-agent": "Algolia for JavaScript (4.24.0); Browser (lite)",
              "x-algolia-api-key": "cdd839b4fdac840289e88633779e8634",
              "x-algolia-application-id": "WD0PTZ13ZS",
          }

          CATEGORIES = {
              "rent_commercial": {"index": "property-for-rent-commercial.com", "filter": '("category_v2.slug_paths":"property-for-rent/commercial")'},
              "rent_holiday_homes": {"index": "property-for-rent-holiday-homes.com", "filter": '("categories.slug_paths":"property-for-rent/holiday-homes")'},
              "rent_short_term_monthly": {"index": "property-for-rent-short-term.com", "filter": '("categories_v2.slug_paths":"property-for-rent/short-term")', "hits": []},
              "rent_short_term_daily": {"index": "property-for-rent-short-term-daily.com", "filter": '("category_v2.slug_paths":"property-for-rent/short-term-daily")'},
          }
          
          ROWS_PER_BATCH = 2000
          HITS_PER_PAGE = 35
          PAGES_PER_BATCH = math.ceil(ROWS_PER_BATCH / HITS_PER_PAGE)

          matrix = {"include": []}

          for category_name, category in CATEGORIES.items():
              try:
                  payload = {
                      "requests": [{
                          "indexName": category["index"],
                          "query": "",
                          "params": f"page=0&hitsPerPage={HITS_PER_PAGE}&filters={category['filter']}",
                      }]
                  }
                  r = requests.post(URL, params=PARAMS, json=payload, timeout=30)
                  data = r.json()
                  nb_pages = data["results"][0]["nbPages"]
              except Exception as e:
                  print(f"  Failed to detect pages for {category_name}: {e}")
                  continue

              if nb_pages == 0:
                  continue
              #nb_pages = 2
              n_batches = math.ceil(nb_pages / PAGES_PER_BATCH)
              print(f"{category_name}: {nb_pages} pages -> {n_batches} batches")

              for b in range(n_batches):
                  page_start = b * PAGES_PER_BATCH
                  page_end = min((b + 1) * PAGES_PER_BATCH - 1, nb_pages - 1)
                  matrix["include"].append({
                      "category": category_name,
                      "batch_index": str(b),
                      "page_start": str(page_start),
                      "page_end": str(page_end)
                  })

          print(f"Total merge batches: {len(matrix['include'])}")
          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f"matrix={json.dumps(matrix)}\n")
          EOF
  
  merge_batch:
    runs-on: ubuntu-latest
    needs: [detect_merge_batches, phone_scrape]

    strategy:
      max-parallel: 10
      fail-fast: false
      matrix: ${{ fromJson(needs.detect_merge_batches.outputs.matrix) }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas openpyxl boto3 requests pillow python-dotenv

      - name: Download raw data for ${{ matrix.category }}
        uses: actions/download-artifact@v4
        with:
          pattern: raw-${{ matrix.category }}-*
          merge-multiple: true
          path: raw_parts/

      - name: Download phone data
        uses: actions/download-artifact@v4
        with:
          pattern: phones-*
          merge-multiple: true
          path: phones_parts/
        continue-on-error: true

      - name: Process batch (no R2 upload yet)
        env:
          CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
          CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
          CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
          CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
        run: |
          python << 'EOF'
          import glob
          import re
          import shutil
          import pandas as pd
          from dub_writer_prop import process_category

          category = "${{ matrix.category }}"
          page_start = int("${{ matrix.page_start }}")
          page_end = int("${{ matrix.page_end }}")

          # Build slug -> phone lookup from the sequential Camoufox jobs
          phone_lookup = {}
          for pf in glob.glob('phones_parts/*_with_phone_*.csv'):
              try:
                  pdf = pd.read_csv(pf, dtype={'slug': str, 'contact_phone_number': str})
              except Exception as e:
                  print(f"  [WARN] Could not read {pf}: {e}")
                  continue
              if 'slug' not in pdf.columns or 'contact_phone_number' not in pdf.columns:
                  continue
              for _, prow in pdf.iterrows():
                  if pd.notna(prow.get('contact_phone_number')):
                      phone_lookup[str(prow['slug'])] = prow['contact_phone_number']
          print(f"  Loaded {len(phone_lookup)} phone numbers")

          all_jsonl = glob.glob('raw_parts/*.jsonl')
          batch_jsonl = []
          for f in all_jsonl:
              match = re.search(r'_(\d+)_(\d+)\.jsonl$', f)
              if match:
                  s, e = int(match.group(1)), int(match.group(2))
                  if s <= page_end and e >= page_start:
                      batch_jsonl.append(f)

          print(f"Batch {category} [{page_start}-{page_end}]: {len(batch_jsonl)} raw files")

          if not batch_jsonl:
              print("No data for this batch, skipping.")
              exit(0)

          output_base = "output"
          result = process_category(category, jsonl_files, output_base, upload_images=True,
                                    enrich_contact_details = True, phone_lookup=phone_lookup)
                                    
          print(f"Batch processed: {result['total']} rows | {len(result['excel_files'])} excel files")
          
          if not result["excel_files"]:
            print("No output files generated. Skipping batch.")
            exit(0)

          for stats_file in glob.glob('raw_parts/request_stats_*.json'):
              m = re.search(r'request_stats_(\d+)_(\d+)\.json$', stats_file)
              if m:
                  s, e = int(m.group(1)), int(m.group(2))
                  if s <= page_end and e >= page_start:
                      shutil.copy(stats_file, f"output/_stats_{page_start}_{page_end}.json")
          EOF

      - name: Upload batch artifact
        uses: actions/upload-artifact@v4
        with:
          name: batch-${{ matrix.category }}-${{ matrix.batch_index }}
          path: output/
          if-no-files-found: ignore

  final_merge:
    runs-on: ubuntu-latest
    needs: merge_batch

    strategy:
      max-parallel: 10
      matrix:
        category:
          - rent_commercial
          - rent_holiday_homes
          - rent_short_term_daily
          - rent_short_term_monthly

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas openpyxl boto3

      - name: Download all batches for ${{ matrix.category }} (separate folders)
        uses: actions/download-artifact@v4
        with:
          pattern: batch-${{ matrix.category }}-*
          path: batches/

      - name: Combine batches and upload to R2
        env:
          CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
          CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
          CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
          CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
        run: |
          python << 'EOF'
          import glob
          import os
          import json
          import boto3
          import pandas as pd
          from datetime import datetime
          from collections import defaultdict

          category = "${{ matrix.category }}"
          dt = datetime.now()
          date_prefix = f"year={dt.year}/month={dt.strftime('%m')}/day={dt.strftime('%d')}"

          client = boto3.client('s3',
              endpoint_url=os.environ['CF_R2_ENDPOINT_URL'],
              aws_access_key_id=os.environ['CF_R2_ACCESS_KEY_ID'],
              aws_secret_access_key=os.environ['CF_R2_SECRET_ACCESS_KEY'],
              region_name='auto'
          )
          bucket = os.environ['CF_R2_BUCKET_NAME']

          batch_dirs = glob.glob(f'batches/batch-{category}-*')

          if not batch_dirs:
            print(f"No batches found for {category}, skipping upload.")
            exit(0)

          excel_groups = defaultdict(list)   # relative_path -> [full_path1, full_path2, ...]

          for batch_dir in batch_dirs:
              for root, dirs, files in os.walk(batch_dir):
                  for file in files:
                      if file.endswith('.xlsx'):
                          full_path = os.path.join(root, file)
                          relative_path = os.path.relpath(full_path, batch_dir)
                          excel_groups[relative_path].append(full_path)

          print(f"Found {len(excel_groups)} unique file groups to combine")

          total_rows_all = 0
          uploaded_count = 0

          for relative_path, batch_files in excel_groups.items():
              combined_sheets = defaultdict(list)

              for bf in batch_files:
                  xl = pd.ExcelFile(bf)
                  for sheet in xl.sheet_names:
                      df = xl.parse(sheet, dtype={'contact_phone_number': str})
                      combined_sheets[sheet].append(df)

              final_sheets = {}
              for sheet_name, dfs in combined_sheets.items():
                  merged_df = pd.concat(dfs, ignore_index=True)
                  if 'id' in merged_df.columns:
                      merged_df = merged_df.drop_duplicates(subset=['id'], keep='first')
                  final_sheets[sheet_name] = merged_df
                  total_rows_all += len(merged_df)

              local_final_path = os.path.join("final_output", relative_path)
              os.makedirs(os.path.dirname(local_final_path), exist_ok=True)

              with pd.ExcelWriter(local_final_path, engine="openpyxl") as writer:
                  for sheet_name, df in final_sheets.items():
                      df.to_excel(writer, sheet_name=sheet_name, index=False)

              json_relative = relative_path.replace('/excel/', '/json/').replace('.xlsx', '.json')
              local_json_path = os.path.join("final_output", json_relative)
              os.makedirs(os.path.dirname(local_json_path), exist_ok=True)

              all_records = []
              for sheet_name, df in final_sheets.items():
                  records = df.to_dict(orient="records")
                  for r in records:
                      r["_sheet"] = sheet_name
                  all_records.extend(records)

              with open(local_json_path, "w", encoding="utf-8") as f:
                  json.dump(all_records, f, ensure_ascii=False, indent=2, default=str)

              r2_key_excel = f"DUAE/{date_prefix}/{relative_path.replace(os.sep, '/')}"
              client.upload_file(local_final_path, bucket, r2_key_excel,
                  ExtraArgs={'ContentType': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'})
              print(f"Uploaded Excel: {r2_key_excel}")

              r2_key_json = f"DUAE/{date_prefix}/{json_relative.replace(os.sep, '/')}"
              client.upload_file(local_json_path, bucket, r2_key_json,
                  ExtraArgs={'ContentType': 'application/json; charset=utf-8'})
              print(f"Uploaded JSON: {r2_key_json}")

              uploaded_count += 1

          # REQUEST STATS
          def aggregate_request_stats(paths: list) -> dict:
              total_requests = 0
              total_duration_min = 0.0
              for p in paths:
                  try:
                      with open(p, 'r', encoding='utf-8') as fh:
                          s = json.load(fh)
                      total_requests += s.get('total_requests', 0)
                      total_duration_min += s.get('total_duration_min', 0) or 0
                  except Exception as e:
                      print(f"Could not read {p}: {e}")
              requests_per_min = round(total_requests / total_duration_min, 2) if total_duration_min > 0 else total_requests
              return {
                  "requests_total": total_requests,
                  "duration_sec": round(total_duration_min * 60, 2),
                  "requests_per_min": requests_per_min,
              }

          stats_files = []
          for batch_dir in batch_dirs:
              stats_files.extend(glob.glob(os.path.join(batch_dir, "_stats_*.json")))

          request_metrics = aggregate_request_stats(stats_files)
          print(f"Request metrics for {category}: {request_metrics['requests_total']} req, "
                f"{request_metrics['requests_per_min']} req/min, {request_metrics['duration_sec']}s")

          # FAILED PAGES
          all_failed_pages = []
          total_pages_all = 0

          for batch_dir in batch_dirs:
              for failed_json in glob.glob(os.path.join(batch_dir, "_batch_failed_*.json")):
                  with open(failed_json, 'r', encoding='utf-8') as f:
                      info = json.load(f)
                      all_failed_pages.extend(info.get("failed_pages", []))
                      total_pages_all += info.get("total_pages_in_batch", 0)

          all_failed_pages = sorted(set(all_failed_pages))
          failed_pct = round(len(all_failed_pages) / total_pages_all * 100, 2) if total_pages_all > 0 else 0

          failed_data = {
              "category": category,
              "total_pages_attempted": total_pages_all,
              "total_failed": len(all_failed_pages),
              "failed_percentage": failed_pct,
              "failed_pages": all_failed_pages,
          }

          # UPLOAD summary.json / failed.json / request_metrics.json
          # per group (cat0/cat1/extra_folder hierarchy)

          summary_hierarchy_groups = set()
          for relative_path in excel_groups.keys():
              parts = relative_path.replace(os.sep, '/').split('/')
              hierarchy = "/".join(parts[:-2])  # drop {excel_or_json}/{filename}
              summary_hierarchy_groups.add(hierarchy)

          # Merge each batch's own summary.json (per exact group) instead of
          # re-deriving totals from summary.txt -- read every batch's
          # summary.json under this category and re-aggregate listings_count.
          summary_by_group = {}
          for batch_dir in batch_dirs:
              for summary_file in glob.glob(os.path.join(batch_dir, "**", "summary.json"), recursive=True):
                  rel = os.path.relpath(summary_file, batch_dir)
                  hierarchy = os.path.dirname(os.path.dirname(rel)).replace(os.sep, '/')
                  with open(summary_file, 'r', encoding='utf-8') as f:
                      s = json.load(f)
                  agg = summary_by_group.setdefault(hierarchy, {
                      "scraped_at": s["scraped_at"],
                      "data_scraped_date": s["data_scraped_date"],
                      "saved_to_R2_date": s["saved_to_R2_date"],
                      "category": s.get("category", ""),
                      "total_listings": 0,
                      "subcats": {},
                  })
                  agg["total_listings"] += s.get("total_listings", 0)
                  for sc in s.get("subcategories", []):
                      key = sc["slug"]
                      if key in agg["subcats"]:
                          agg["subcats"][key]["listings_count"] += sc["listings_count"]
                      else:
                          agg["subcats"][key] = dict(sc)

          for hierarchy, agg in summary_by_group.items():
              summary_data = {
                  "scraped_at": agg["scraped_at"],
                  "data_scraped_date": agg["data_scraped_date"],
                  "saved_to_R2_date": agg["saved_to_R2_date"],
                  "category": agg["category"],
                  "total_subcategories": len(agg["subcats"]),
                  "total_listings": agg["total_listings"],
                  "subcategories": list(agg["subcats"].values()),
              }
              r2_key = f"DUAE/{date_prefix}/{hierarchy}/summary/summary.json"
              client.put_object(Bucket=bucket, Key=r2_key, Body=json.dumps(summary_data, ensure_ascii=False, indent=2).encode('utf-8'),
                               ContentType='application/json; charset=utf-8')
              print(f"Uploaded summary: {r2_key}")

          for hierarchy in summary_hierarchy_groups:
              failed_key = f"DUAE/{date_prefix}/{hierarchy}/summary/failed.json"
              client.put_object(Bucket=bucket, Key=failed_key, Body=json.dumps(failed_data, ensure_ascii=False, indent=2).encode('utf-8'),
                               ContentType='application/json; charset=utf-8')
              print(f"Uploaded: {failed_key}")

              metrics_key = f"DUAE/{date_prefix}/{hierarchy}/summary/request_metrics.json"
              client.put_object(Bucket=bucket, Key=metrics_key, Body=json.dumps(request_metrics, ensure_ascii=False, indent=2).encode('utf-8'),
                               ContentType='application/json; charset=utf-8')
              print(f"Uploaded: {metrics_key}")

          print(f"\nDONE: {uploaded_count} files uploaded for {category} | Total rows: {total_rows_all}")
          EOF

#   compress_images:
#     runs-on: ubuntu-latest
#     needs: final_merge

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - run: pip install boto3

#       - name: Compress images
#         env:
#           CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
#           CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
#           CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
#           CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
#         run: |
#             python << 'EOF'
#             import os
#             import io
#             import boto3
#             import zipfile
#             from datetime import datetime
#             from collections import defaultdict

#             dt = datetime.now()
#             date_prefix = f"year={dt.year}/month={dt.strftime('%m')}/day={dt.strftime('%d')}"

#             client = boto3.client(
#                 "s3",
#                 endpoint_url=os.environ["CF_R2_ENDPOINT_URL"],
#                 aws_access_key_id=os.environ["CF_R2_ACCESS_KEY_ID"],
#                 aws_secret_access_key=os.environ["CF_R2_SECRET_ACCESS_KEY"],
#                 region_name="auto",
#             )

#             bucket = os.environ["CF_R2_BUCKET_NAME"]

#             base_prefix = f"DUAE/{date_prefix}/"

#             all_image_keys = []

#             paginator = client.get_paginator("list_objects_v2")
#             for page in paginator.paginate(Bucket=bucket, Prefix=base_prefix):
#                 for obj in page.get("Contents", []):
#                     key = obj["Key"]
#                     if "/images/" in key and not key.endswith(".zip"):
#                         all_image_keys.append(key)

#             if not all_image_keys:
#                 print("No images found to compress")
#                 exit(0)

#             images_by_folder = defaultdict(list)

#             for key in all_image_keys:
#                 folder = key.rsplit("/", 1)[0]
#                 images_by_folder[folder].append(key)

#             print(f"Found {len(images_by_folder)} image folders to compress")

#             for folder, keys in images_by_folder.items():

#                 print(f"\nCompressing {len(keys)} images in {folder}...")

#                 with io.BytesIO() as zip_buffer:

#                     uploaded_count = 0

#                     with zipfile.ZipFile(
#                         zip_buffer,
#                         mode="w",
#                         compression=zipfile.ZIP_DEFLATED,
#                     ) as zf:

#                         for key in keys:

#                             filename = key.rsplit("/", 1)[-1]

#                             try:
#                                 obj = client.get_object(Bucket=bucket, Key=key)
#                                 body = obj["Body"]

#                                 try:
#                                     zf.writestr(filename, body.read())
#                                     uploaded_count += 1
#                                 finally:
#                                     body.close()

#                             except Exception as e:
#                                 print(f"    [ERROR] Failed to read {key}: {e}")

#                     if uploaded_count == 0:
#                         print("    No images were added to the zip, skipping folder.")
#                         continue

#                     zip_buffer.seek(0)

#                     zip_r2_key = f"{folder}/images.zip"

#                     client.upload_fileobj(
#                         zip_buffer,
#                         bucket,
#                         zip_r2_key,
#                         ExtraArgs={
#                             "ContentType": "application/zip"
#                         },
#                     )

#                     response = client.head_object(
#                         Bucket=bucket,
#                         Key=zip_r2_key,
#                     )

#                     print(
#                         f"    Uploaded zip: {zip_r2_key} "
#                         f"({uploaded_count} images, {response['ContentLength']} bytes)"
#                     )

#                     delete_objects = [{"Key": k} for k in keys]

#                     for i in range(0, len(delete_objects), 1000):
#                         client.delete_objects(
#                             Bucket=bucket,
#                             Delete={
#                                 "Objects": delete_objects[i:i + 1000]
#                             },
#                         )

#                     print(f"    Deleted {len(keys)} individual images from {folder}")

#             print("\nDONE: Compressed all image folders")
#             EOF